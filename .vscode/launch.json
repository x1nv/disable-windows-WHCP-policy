{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bazel-bin/src/encoding/encoding.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build:debug"
        }
    ]
}