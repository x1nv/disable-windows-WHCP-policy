{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug: disable-WHCP",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/src/disable-WHCP.ps1",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}